# Dashboard

This folder is reserved for the Svelte-based graph and drift-event dashboard described in the architecture notes.

The backend API is scaffolded first so the dashboard can be built against stable endpoints instead of placeholders.
